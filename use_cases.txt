bool receive_schema(user, task, file_name){
	- получаем схему
	- провер€ем лексически(на корректнось)
	- провер€ем на тестах 
	- добавл€ем в "€щик"(просто название дл€ хранилища)
	- возвращаем результат проверки
}

bool give_result(user, task, schema_number){
	- schema_number - номер(а не ID) (например 3-€ посылка ¬аси по задаче A)
	- получает ID по этим параметрам
	- берет результат из "€щика"
}

bool give_schema(user, task, schema_number){
	- получает ID
	- берет схему из "€щика"
}