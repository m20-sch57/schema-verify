bool receive_schema(user, task, file_name){
	- получаем схему
	- проверяем лексически(на корректнось)
	- проверяем на тестах 
	- добавляем в "ящик"(просто название для хранилища)
	- возвращаем результат проверки
}

bool give_result(user, task, schema_number){
	- schema_number - номер(а не ID) (например 3-я посылка Васи по задаче A)
	- получает ID по этим параметрам
	- берет результат из "ящика"
}

schema give_schema(user, task, schema_number){
	- получает ID
	- берет схему из "ящика"
}

bool register(login, password){
	- проверяем есть ли такой пользователь
	- добавляем в "ящик"
}
